1.
    Добавил необходимые контролллеры. Чуть больше о них:
     Всего существует 3 вида контроллеров
        a. Unmarhalling
        b. Calulate
        c. Marhalling

        Что они делают по названию думаю ясно.

            Для того чтобы добавление нового котроллера не вызывало проблем - поведение каждого из них декларированно
            рядом интерфейсов.

            Т.к сами по себе контроллеры связаны данными и их обработкой, каждый контроллер имеет метод - execute,
            который в зависимости от контроллера возвращает данные, необходимые дла работы следующего котроллера.
             main() -> Unm -> Cal -> Mar ->  void

              DAO -> Unm -> DAO
              DAO -> Cal -> List<T>
              List<T> -> Mar -> void.


              Calculate:
               Предполагая, что некоторые вычисления потребуют работу с несколькими ДАО одновременно - прописал в шаблоне
               переменное кол-во DAO. ( public void setDAO(T... dao); )

2.
a. Теперь DAO не обезательно парсят данные в конструкторе. Появились методы setList и setListByParsing. Т.е теперь они могут                                                         
    собой представлять что-то типо контейнера для работы
    с листом данных.
    b. Большая часть параметров парсинга находится в XML геттерах. Доступна настройка через setProperties.

    Также было несколько заведомо нацеленных упрощений конструкторов и функций в целом. Большая чатсь вышеперечисленного была     
    сдеаланна
    с целью сделать код более тестируемым.
    ( "написанные" мною убогие юнит тесты, прошу в проект не рассматривать. Это была просто проверка)

3.
    Теперь ссылки берутся из routes.properties. JSON больше нигде не используется. Однако из проекта не удалял.


4.
    Прикрутил log4j. Логи выводятся как в консоль так и в файл - progress.log. До этого лоигрвоанием серьезно не занимался, 
    поэтому о качестве судить не могу.

5. Что касательно работы самого проекта. Нужно просто запустить файл main.
        минус - для парсинга файлов выходит много кода.
        Из + или -. Выделил бы то, что для парсинга новых файлов все контроллеры принаджлижащие к нововому парсингу придется 
        упаковывать в новый try-catch


6. Орфографию посмотрел, где-то исправил, где-то возможно упустил. Пакеты переименовал в нижний регистр.
 (Кроме DAO. DAO - не хочет он переименовываться. Не знаю почему, Idea никак не хочет.)

________________________________________________________________________________________________________________________________________________

Надеюсь никаких серьезных ошибок не допустил. Вроде (как мне кажется) все исключения отлавливаются, лоигруются.


